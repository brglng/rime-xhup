# Rime dict
# encoding: utf-8

---
name: xhup
version: "0.1"
sort: by_weight
use_preset_vocabulary: true
import_tables:
  - xhup
max_phrase_length: 14
encoder:
  exclude_patterns:
    - '^o.*$'
    - '^`.*$'
  rules:
    - length_equal: 2
      formula: "AaAbZaZb"
    - length_equal: 3
      formula: "AaBaZaZb"
    - length_in_range: [4, 14]
      formula: "AaBaCaZa"
...

# copyied from luna_pinyin.dict.yaml
